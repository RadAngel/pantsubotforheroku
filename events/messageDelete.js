const Discord = require('discord.js');

module.exports.eventrun = async (bot,messageDeleted) => {
    // console.log(`Mensaje borrado en ${messageDeleted.guild.name} de ${messageDeleted.author.username}`)
}