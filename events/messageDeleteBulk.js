const Discord = require('discord.js');

module.exports.eventrun = async (bot,messagesDeleted) => {
    // console.log("bulk")
    // console.log(`BulkDelete en ${messagesDeleted.map(m => m.guild.name)[0]} cantidad ${messagesDeleted.size}`)
    // console.log(messagesDeleted.map(m => m.content))
    // logschannel = bot.channels.get("464966590794825748")
    // logschannel.send(`Se han borrado ${messagesDeleted.size} mensajes en el canal ${messagesDeleted.map(c => c.channel.name)[0]} en el servidor ${messagesDeleted.map(g => g.guild.name)[0]}`)
}